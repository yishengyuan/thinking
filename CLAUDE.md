# CLAUDE.md

## 这个项目是什么

`thinking` 是一个**个人思考沉淀仓库**,用来记录两类东西:

- **想法 / Ideas(`docs/想法/`)**:技术方案、架构探讨、各种点子。重在「表达想法」,
  不追求一定能落地,允许是半成品的思路探讨。
- **反思 / Reflections(`docs/反思/`)**:工作中踩的坑、复盘、经验教训。
  目标是把具体事件沉淀成「以后能拿出来看的教材」,避免重复犯错。

简单说:**这里既装 idea,也装反思。** 两者都是「思考」的产物。

## 目录约定

```
docs/
├── 想法/      # ideas:技术方案、架构、点子
└── 反思/      # reflections:复盘、经验教训、教材
```

## 写作约定

- **语言**:中文为主,技术术语可保留英文。
- **流程图**:优先用 mermaid(flowchart / sequence 等)把思路画清楚,文字做必要补充。
- **反思类文档结构**:尽量遵循「事件经过 → 问题分析 → 根因 → 改进行动」,
  让它真正能当复盘教材用,而不只是发牢骚。
- **想法类文档**:可以是开放式探讨,结尾留「后续可展开的点」。

## 环境备注(Claude 使用)

- **用户使用 Claude 的操作系统是 Windows。** 本仓库里讨论的各种「Claude 使用技巧 / 工具 /
  命令 / 安装 / 路径 / 快捷键」,**默认都按 Windows 环境来**给(例如路径用反斜杠、命令用
  PowerShell/CMD 或 WSL,而非默认 macOS/Linux)。涉及示例时以 Windows 为准,必要时再注明其他平台。
- 注:云端会话(Claude Code on the web)跑在 Linux 沙箱里,这与"用户本机是 Windows"是两回事;
  给本机操作建议时按 Windows,给云沙箱内命令时按其 Linux 环境。

## 给未来 Claude 的提示

- 新增内容时先判断属于「想法」还是「反思」,放进对应目录。
- 不要过度修饰用户的原始表达;反思类内容的价值在于真实,保留具体场景和细节。
