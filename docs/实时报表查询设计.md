# 用实时表查询替代定时任务 —— 报表查询设计的思路探讨

> 这是一份「想法记录」,把这次讨论的演进过程沉淀下来:
> 从「定时任务跑批」到「直接查实时表」的动机,
> MySQL 从库与 ClickHouse(CK)两条路线的取舍,
> 以及实时报表 + 历史报表跑批的整体设计。
> 以流程图为主线,文字只做必要的补充。

---

## 1. 缘起:为什么想用「查实时表」替代「定时任务」

最初的报表是用**定时任务(cron / 跑批)**预先把数据聚合好,落到一张结果表里,
前端查的永远是「上一次跑批的快照」。这套方案能跑,但用久了痛点很明显:

- **实时性差**:数据要等下一个调度周期(小时级 / 天级)才更新。
- **任务多、链路长**:维度一多,跑批脚本越堆越多,补数、重跑、依赖编排都很重。
- **故障放大**:一个上游任务失败,下游一串报表全空,排查成本高。
- **资源浪费**:很多维度其实没人看,却天天在跑。

于是有了第一个念头 —— **能不能不预聚合,直接查实时表,把结果实时算出来?**

```mermaid
flowchart LR
    subgraph OLD["旧方案:定时任务预聚合"]
        A1[业务库] --> A2[定时跑批<br/>cron/调度]
        A2 --> A3[(结果快照表)]
        A3 --> A4[报表前端]
    end

    subgraph NEW["新思路:直接查实时表"]
        B1[业务库] --> B2[实时同步]
        B2 --> B3[(实时查询表)]
        B3 --> B4[查询时实时聚合]
        B4 --> B5[报表前端]
    end

    OLD -. 演进 .-> NEW

    style OLD fill:#f5f5f5,stroke:#bbb
    style NEW fill:#e8f5e9,stroke:#4caf50
```

核心转变:**把「写时计算」(跑批时算好)改成「读时计算」(查询时再算)。**
代价是查询要扛得住实时聚合的压力 —— 这正是下面选型要解决的问题。

---

## 2. 实时表从哪来:MySQL 从库 vs ClickHouse

「查实时表」首先要回答一个问题:**这张实时表放在哪、用什么引擎?**
讨论里出现了两条路线 —— 直接查 **MySQL 从库**,或者把数据同步到 **ClickHouse**。

两条路线的共同前提是**不能压主库**,所以数据都要先从主库的 binlog 流出来。

```mermaid
flowchart TD
    M[(MySQL 主库<br/>承载业务写入)]
    M -->|binlog| R[(MySQL 从库<br/>主从复制)]
    M -->|binlog 订阅<br/>Canal/Debezium 等| SYNC[同步链路]
    SYNC --> CK[(ClickHouse<br/>列式 OLAP)]

    R --> Q1{报表查询}
    CK --> Q1

    style M fill:#ffe0b2,stroke:#fb8c00
    style R fill:#e3f2fd,stroke:#1e88e5
    style CK fill:#f3e5f5,stroke:#8e24a5
```

### 2.1 两条路线的特点对比

| 维度 | MySQL 从库 | ClickHouse(CK) |
|------|-----------|----------------|
| 存储模型 | 行式,面向 OLTP | 列式,面向 OLAP |
| 擅长场景 | 点查、按索引的小范围聚合 | 大数据量扫描 / 多维聚合 |
| 实时性 | 主从延迟通常很低(秒级) | 取决于同步链路,通常秒级~分钟级 |
| 大表聚合性能 | 数据量大时明显吃力 | 强,压缩比高、向量化执行 |
| 写入/更新 | 天然支持事务更新 | 更新代价高,适合追加 |
| 改造成本 | 低(已有从库即可) | 需新增同步链路 + 运维 CK |
| 一致性 | 与主库强一致(复制延迟内) | 最终一致,依赖同步链路 |

### 2.2 选型的判断思路

不是「二选一」,而是**按查询特征路由**:

```mermaid
flowchart TD
    START[一个报表查询] --> Q1{数据量 / 扫描行数大吗?}
    Q1 -->|小:点查或<br/>窄范围聚合| MYSQL[走 MySQL 从库<br/>低成本、强一致]
    Q1 -->|大:全表扫描 /<br/>多维大聚合| Q2{对实时性要求多高?}

    Q2 -->|秒级最终一致<br/>可接受| CK[走 ClickHouse<br/>聚合性能强]
    Q2 -->|要求与主库<br/>强一致| Q3{能否限定<br/>查询范围/走索引?}

    Q3 -->|能| MYSQL
    Q3 -->|不能,且量大| CK

    style MYSQL fill:#e3f2fd,stroke:#1e88e5
    style CK fill:#f3e5f5,stroke:#8e24a5
```

一句话总结取舍:
**小而精、要强一致 → MySQL 从库;大而广、重聚合分析 → ClickHouse。**

---

## 3. 报表查询设计:实时报表 + 历史报表跑批

真正落地报表时,发现「全部实时算」也不现实 ——
**历史数据已经固化、不再变化,每次都重算是浪费**;
而**当天/近期数据频繁变动,才真正需要实时**。

于是回到一个混合设计(本质是 Lambda 架构的思路):
**冷热分离 —— 历史走跑批预聚合,实时走实时表查询,查询层做合并。**

### 3.1 冷热分层

```mermaid
flowchart LR
    subgraph HOT["热数据(近期 / 当天)"]
        H1[实时表<br/>MySQL从库 或 CK] --> H2[查询时实时聚合]
    end

    subgraph COLD["冷数据(历史 / 已封账)"]
        C1[跑批预聚合] --> C2[(历史结果表)]
    end

    H2 --> MERGE[查询层合并<br/>历史 + 实时]
    C2 --> MERGE
    MERGE --> RPT[报表结果]

    style HOT fill:#ffebee,stroke:#e53935
    style COLD fill:#e1f5fe,stroke:#039be5
    style MERGE fill:#fff8e1,stroke:#fbc02d
```

要点:

- **历史数据只跑一次**:某天数据封账后预聚合落表,以后直接读,不再重算。
- **实时数据按需算**:只对「热区间」做实时聚合,数据量小,压力可控。
- **分界线**:用一个时间边界(如「今天 00:00」或「最近 N 天」)切分冷热,
  避免重叠计算或漏算。

### 3.2 一次报表查询的路由流程

```mermaid
flowchart TD
    REQ[报表查询请求<br/>含时间范围] --> SPLIT{时间范围<br/>跨冷热边界?}

    SPLIT -->|全部落在历史区| H[读历史预聚合结果表]
    SPLIT -->|全部落在实时区| R[查实时表实时聚合]
    SPLIT -->|跨边界| BOTH[拆成两段]

    BOTH --> H2[历史段:读结果表]
    BOTH --> R2[实时段:查实时表]
    H2 --> COMBINE[合并两段结果]
    R2 --> COMBINE

    H --> OUT[返回报表]
    R --> OUT
    COMBINE --> OUT

    style H fill:#e1f5fe,stroke:#039be5
    style R fill:#ffebee,stroke:#e53935
    style COMBINE fill:#fff8e1,stroke:#fbc02d
    style OUT fill:#e8f5e9,stroke:#43a047
```

### 3.3 历史跑批的角色变化

注意:引入实时表**并没有完全消灭定时任务**,而是**给它重新定位**:

- 旧角色:跑批是**唯一**的数据来源,实时性全靠它,所以又多又重。
- 新角色:跑批只负责**把昨天及更早的数据固化成历史结果表**,
  跑一次、跑得稳,不再背实时性的包袱。

```mermaid
flowchart LR
    subgraph BEFORE["改造前"]
        X1[定时任务] --> X2[同时承担:<br/>实时 + 历史]
    end
    subgraph AFTER["改造后"]
        Y1[实时表查询] --> Y2[负责:热数据实时性]
        Y3[定时任务] --> Y4[负责:冷数据固化]
    end
    BEFORE -. 职责拆分 .-> AFTER

    style BEFORE fill:#f5f5f5,stroke:#bbb
    style AFTER fill:#e8f5e9,stroke:#4caf50
```

---

## 4. 整体架构串起来

把上面三段合并,得到端到端的视图:

```mermaid
flowchart TD
    SRC[(MySQL 主库<br/>业务写入)]

    SRC -->|主从复制| SLAVE[(MySQL 从库)]
    SRC -->|binlog 订阅| PIPE[同步链路]
    PIPE --> CKDB[(ClickHouse)]

    SRC -.->|每日跑批<br/>固化历史| BATCH[定时跑批]
    BATCH --> HIST[(历史结果表)]

    subgraph QUERY["查询层(按特征路由 + 冷热合并)"]
        ROUTER{查询路由}
    end

    SLAVE --> ROUTER
    CKDB --> ROUTER
    HIST --> ROUTER

    ROUTER --> REPORT[报表前端]

    style SRC fill:#ffe0b2,stroke:#fb8c00
    style SLAVE fill:#e3f2fd,stroke:#1e88e5
    style CKDB fill:#f3e5f5,stroke:#8e24a5
    style HIST fill:#e1f5fe,stroke:#039be5
    style ROUTER fill:#fff8e1,stroke:#fbc02d
    style REPORT fill:#e8f5e9,stroke:#43a047
```

---

## 5. 小结:这次讨论想表达的几个想法

1. **动机**:定时任务预聚合实时性差、维护重,想用「查实时表」替代,把「写时计算」变「读时计算」。
2. **实时表选型**:MySQL 从库 vs ClickHouse 不是二选一,而是按查询特征路由 ——
   小而精强一致走从库,大而广重聚合走 CK,共同前提是不压主库。
3. **报表设计**:全部实时不现实,采用冷热分离 ——
   历史跑批固化、实时表算热数据、查询层合并,定时任务从「全包」退回到「只管历史固化」。

> 后续可以再展开的点:冷热时间边界怎么定、CK 同步链路的一致性与去重、
> 查询层合并的口径对齐(避免重复/遗漏)、降级与缓存策略。
